/**
 * StoreServices.jsx
 * - Store/Services page placeholder
 */
import { setPageTitle } from "../utils/seo.js";

export default function StoreServices() {
  setPageTitle("Store / Services");

  return (
    <section className="container page">
      <h1>Store / Services</h1>
      <p>Placeholder: services, amenities, store items, car wash, ATM, etc.</p>
    </section>
  );
}
