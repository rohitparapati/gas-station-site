/**
 * LocationHours.jsx
 * - Location & Hours page placeholder
 */
import { setPageTitle } from "../utils/seo.js";

export default function LocationHours() {
  setPageTitle("Location & Hours");

  return (
    <section className="container page">
      <h1>Location & Hours</h1>
      <p>Placeholder: address, hours, map embed (later), directions.</p>
    </section>
  );
}
